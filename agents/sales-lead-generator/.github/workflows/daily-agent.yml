name: Daily AI Sales Agent

on:
  schedule:
    - cron: "0 13 * * *"
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      #Checkout repo
      - uses: actions/checkout@v4

      #Setup Python
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      #Install dependencies
      - run: pip install -r requirements.txt

      #Create service account file
      - run: |
          echo "${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}" > service_account.json
          
      #Run AI Agent
      - run: python agent.py
        env: 
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}


# name: Daily Outreach Lead Generation Run

# on:
#   schedule:
#     - cron: '0 7 * * *'  # Automatically fires at 07:00 AM UTC daily
#   workflow_dispatch:   # Maintains a manual trigger mechanism inside your GitHub UI

# jobs:
#   execute-pipeline:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Python Runtime Env
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#           cache: 'pip'
#       - name: Inject Packages and Trigger Pipeline Execution Engine
#         env:
#           OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#           SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
#         run: |
#           cd agents/sales-lead-generator
#           pip install -r requirements.txt
#           python etl.py